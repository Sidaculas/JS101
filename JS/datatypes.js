// Js has 3 types of data: constants as const (can use it only after assigning, also a block statement),
// variables as var (can define anywhere and use it), and let (block statement, best for loops).

const one = 1;
let two = 2;
var three = 3;